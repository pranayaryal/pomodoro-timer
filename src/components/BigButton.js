import React, { useState } from 'react'


const BigButton = () => {
    return (
        <div>
            <button className="btn is-primary">Press</button>
        </div>
    )
}

export default BigButton;